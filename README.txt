==================================================================
The dataset includes the following files:
=========================================

- 'README.txt'

- 'codebook.txt': Shows information about the files and the variable in it

- 'script.R' : R code to reconstruct the output files all_data.csv and summary_data.csv

- 'all_data.csv' : A csv file containing the merged data training and testing 

- 'summary_data.csv' : A csv file containing summary from all_data (grouped by label, subject)